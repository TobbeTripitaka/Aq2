diff --git a/3b_GBM_SWEEP.ipynb b/3b_GBM_SWEEP.ipynb
index e2ae1fb..0525213 100644
--- a/3b_GBM_SWEEP.ipynb
+++ b/3b_GBM_SWEEP.ipynb
@@ -37,6 +37,8 @@
    "metadata": {},
    "outputs": [],
    "source": [
+    "from config import N_OPTUNA_TRIALS, MC_N_RUNS  # config-derived defaults; kept self-sufficient so this cell can run first\n",
+    "\n",
     "RUN_GBM_OPTUNA    = True\n",
     "RUN_GBM_ABLATION  = True\n",
     "RUN_GBM_MC        = True\n",
diff --git a/3c_SIM_SWEEP.ipynb b/3c_SIM_SWEEP.ipynb
index 86cd7ab..2b4f1d1 100644
--- a/3c_SIM_SWEEP.ipynb
+++ b/3c_SIM_SWEEP.ipynb
@@ -35,6 +35,8 @@
    "metadata": {},
    "outputs": [],
    "source": [
+    "from config import N_OPTUNA_TRIALS  # config-derived default; kept self-sufficient so this cell can run first\n",
+    "\n",
     "RUN_JOINT_OPTUNA = True   # set False to reload from existing sim_joint_results.csv\n",
     "\n",
     "# These are used in §1c and §2 — defined here so they are always set first\n",
